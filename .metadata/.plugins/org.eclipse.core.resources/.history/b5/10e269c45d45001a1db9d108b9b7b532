package stuff;
import java.lang.Math;
public class Encrypter {
	private int userNum; 
	public Encrypter (int userEntry)
	{
		this.userNum = userEntry;
	}
	public int encryptedNum = 0;
	for(int i = 1; i<=4; i++)
	{
		userNum += 7;
		encryptedNum += userNum % Math.pow(10, i);
	}
}
